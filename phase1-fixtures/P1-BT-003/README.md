# P1-BT-003 fixture contract

- Authority: `docs/관리데이터/tests.json#P1-BT-003` and the matching Phase 1 design anchor.
- Independent oracle: usage별 fallback 순서, 중복 probe 0, 유한 종료를 검사한다.
- Test data may be generated deterministically by the named test; live save data and network resources are forbidden.
- Run output: `build/phase1-fixtures/P1-BT-003/evidence.json`; a descriptor alone never counts as PASS.
