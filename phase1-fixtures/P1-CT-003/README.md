# P1-CT-003 fixture contract

- Authority: `docs/관리데이터/tests.json#P1-CT-003` and the matching Phase 1 design anchor.
- Independent oracle: usage/category/crop/TEXT I/O와 terminal semantics를 golden과 비교한다.
- Test data may be generated deterministically by the named test; live save data and network resources are forbidden.
- Run output: `build/phase1-fixtures/P1-CT-003/evidence.json`; a descriptor alone never counts as PASS.
