# P1-CT-001 fixture contract

- Authority: `docs/관리데이터/tests.json#P1-CT-001` and the matching Phase 1 design anchor.
- Independent oracle: CSV/JSON dialect, 결정론, stable diagnostic code와 locator를 비교한다.
- Test data may be generated deterministically by the named test; live save data and network resources are forbidden.
- Run output: `build/phase1-fixtures/P1-CT-001/evidence.json`; a descriptor alone never counts as PASS.
